API.on(API.USER_JOIN, function(user) {
     if (user.id == "id") {
         API.sendChat("/me ooo @" + user.username + " oooo ");
         }
         
      if (user.id == "id") {
         API.sendChat("/me oo @" + user.username + " oo ");
         } 
		 
      if (user.id == "id") {
         API.sendChat("/me oo @" + user.username + " oo ");
         }
    
     if (user.id == "id") {
         API.sendChat("/me frase @" + user.username + " frase ");
         }
        });
